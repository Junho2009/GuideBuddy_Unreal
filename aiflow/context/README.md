# context

本目录保存当前 MVP 的长期背景信息。

当前只有一个运行时背景入口：

- `combat-runtime.md`：记录 `SampleDemoShowcaseMap`、TempestCombatFramework、主要资产入口和推荐遥测挂点。

这里不放执行步骤、临时排障或验收记录；那些内容应进入 `contracts/runs/`、`contracts/evidence/` 或后续 result ledger。
